name: Build

on:
  workflow_dispatch:

jobs:
  build-android:
    name: Android APK
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.38.7'
          channel: stable

      - name: Install dependencies
        run: flutter pub get

      - name: Build APK (ARM64 for Android phones)
        run: flutter build apk --debug --target-platform android-arm64
        env:
          GRADLE_OPTS: "-Dorg.gradle.daemon=false"

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: koko-and-me-apk
          path: build/app/outputs/flutter-apk/app-debug.apk
          retention-days: 7

  build-macos:
    name: macOS App
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.38.7'
          channel: stable

      - name: Install dependencies
        run: flutter pub get

      - name: Build macOS app
        run: flutter build macos --release

      - name: Zip .app bundle
        run: |
          ditto -c -k --sequesterRsrc --keepParent \
            "build/macos/Build/Products/Release/koko_and_me.app" \
            "koko-and-me-macos.zip"

      - name: Upload macOS app
        uses: actions/upload-artifact@v4
        with:
          name: koko-and-me-macos
          path: koko-and-me-macos.zip
          retention-days: 14

  build-ios:
    name: iOS IPA (AltStore)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.38.7'
          channel: stable

      - name: Install dependencies
        run: flutter pub get

      - name: Build iOS (no code sign)
        run: flutter build ios --release --no-codesign

      - name: Package as IPA
        run: |
          mkdir -p Payload
          cp -r build/ios/iphoneos/Runner.app Payload/
          zip -r koko-and-me.ipa Payload/

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: koko-and-me-ios
          path: koko-and-me.ipa
          retention-days: 14
